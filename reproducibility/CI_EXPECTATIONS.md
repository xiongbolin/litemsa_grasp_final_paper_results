# CI expectations

The `Public reproducibility check` workflow must pass before this branch is merged into `main`. It installs the public plotting dependency, runs the numeric verifier, regenerates all nine panel plots, and checks the generated file count.
